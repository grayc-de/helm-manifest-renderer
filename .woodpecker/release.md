# helm-manifest-renderer

Release: `{{VERSION}}`

This release contains the `helm-manifest-renderer` CLI as a Linux `amd64`
artifact and its checksum file.

## Changes Since Last Release

{{CHANGELOG}}
